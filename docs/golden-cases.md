# Golden Cases

The golden set keeps `feature drift` and `metric stability` visible as named cases.

The main golden fixture is `fixtures/golden/scoreboard.csv`. The matrix fixture is `fixtures/golden/lane-matrix.csv`. Together they cover `feature drift`, `window width`, `metric stability`, and `explainability` with different score ranges.

The purpose is to make intentional rule changes show up in review.
